const leaderboardEndpoint=import.meta.env.VITE_LEADERBOARD_API_URL,explicitEndpoint=import.meta.env.VITE_GAME_API_URL;
function root(){const raw=explicitEndpoint||leaderboardEndpoint;if(!raw)return null;const u=new URL(raw,window.location.origin);u.pathname=u.pathname.replace(/\/api\/(leaderboard|leaderboards)\/?$/,'');u.search='';return u;}
async function call(path,{token,guildId,method='GET',body}={}){const u=root();if(!u)throw new Error('game_api_not_configured');u.pathname=`${u.pathname.replace(/\/$/,'')}${path}`;if(guildId)u.searchParams.set('guildId',guildId);const headers={Accept:'application/json',Authorization:`Bearer ${token}`};if(method!=='GET'){headers['Content-Type']='application/json';headers['X-Request-Id']=crypto.randomUUID();}const res=await fetch(u.toString(),{method,headers,body:body?JSON.stringify(body):undefined,cache:'no-store'});const data=await res.json().catch(()=>({}));if(!res.ok){const e=new Error(data.error||`Game API ${res.status}`);e.status=res.status;e.data=data;throw e}return data;}
export const fetchGameMe=(token,guildId)=>call('/api/game/me',{token,guildId});
export const fetchPortal=(token,guildId)=>call('/api/game/portal',{token,guildId});
export const gameAction=(token,guildId,action,params={},locale='vi')=>call('/api/game/action',{token,guildId,method:'POST',body:{action,params,locale}});
